template(name="tweetCreate")
  if currentUser
    form(class="js-tweet-new row form-group")
      .col-xs-10
        input(type="text" placeholder="What are you thinking?", class="form-control", id="tweet-text")
      .col-xs-2
        input(type="submit", value="Tweet now!", class="btn btn-success js-tweet-add")
